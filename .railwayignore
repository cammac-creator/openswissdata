node_modules
dist
/data
coverage
.env
.env.*
!.env.example

web/node_modules
web/.astro
web/dist
permissions-emails
docs
tests
etl
scripts
presentation

.git
.DS_Store
*.log
*.db
*.db-journal
*.db-wal

# SDKs (sdk-ts, sdk-py, mcp-server) are published independently to npm/PyPI;
# they are not loaded by the Hono runtime, so excluding them keeps the
# Railway upload under a few hundred MB (was timing out on slow uplinks).
sdks
.design-preview
