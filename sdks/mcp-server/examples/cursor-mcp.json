{
  "_comment": "Drop this into ~/.cursor/mcp.json (or the workspace-level .cursor/mcp.json). Restart Cursor after editing.",
  "mcpServers": {
    "openswissdata": {
      "command": "npx",
      "args": ["-y", "@openswissdata/mcp"],
      "env": {
        "OPENSWISSDATA_API_KEY": "sk_live_replace_me_or_remove_for_anonymous_tier"
      }
    }
  }
}
