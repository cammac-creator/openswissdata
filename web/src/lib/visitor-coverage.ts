import type { VisitorCoverage } from '../../../src/lib/crm-audience';

export function renderVisitorCoverage(identity: VisitorCoverage): string {
  const n = (value: number) => new Intl.NumberFormat('fr-CH').format(value);
  return `<section class="panel" style="margin-bottom:24px"><div class="panel-heading"><h2>Ce que couvre l’estimation des visiteurs</h2></div><div class="metric-row"><span>Pages avec un identifiant quotidien actuel</span><strong>${n(identity.current_pages)}</strong></div><div class="metric-row"><span>Pages avec un identifiant historique</span><strong>${n(identity.historical_pages)}</strong></div><div class="metric-row"><span>Pages sans identifiant exploitable</span><strong>${n(identity.unidentified_pages)}</strong></div>${identity.unidentified_pages ? '<p class="notice">Certaines pages ont été enregistrées sans pouvoir estimer leur visiteur. Elles restent comptées dans les pages ; les visiteurs correspondants ne sont pas inventés. Si aucun identifiant n’est disponible, l’estimation affiche un tiret.</p>' : ''}<p class="source-note">Le calcul actuel utilise l’adresse transmise par notre hébergeur et tourne à minuit en Suisse. Le calcul historique tournait à minuit UTC et pouvait être influencé par un en-tête déclaré ; il est conservé sans réécriture. Ces pseudonymes ne sont ni des personnes uniques ni des identifiants anonymes. Une modification de clé ou la transition entre les deux calculs peut compter plusieurs fois une même visite.</p></section>`;
}
