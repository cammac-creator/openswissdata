import type {OrderJourney} from '../../../src/lib/order-journey';
const n=(v:number)=>new Intl.NumberFormat('fr-CH').format(v);
const date=(v:number)=>new Intl.DateTimeFormat('fr-CH',{timeZone:'Europe/Zurich',dateStyle:'medium',timeStyle:'short'}).format(v);

export function renderOrderJourney(j:OrderJourney|null|undefined){
 if(!j)return '<section class="panel" id="order-journey"><h2>Après l’achat</h2><p>Lecture des preuves de service indisponible. Actualisez pour réessayer.</p></section>';
 return `<section class="panel order-journey" id="order-journey" aria-labelledby="order-journey-title"><div class="panel-heading"><div><h2 id="order-journey-title">Après l’achat</h2><p>Commandes créées dans la période choisie, actuellement payées. Achats réels, comptes internes exclus.</p></div></div><div class="journey-summary"><div><span class="journey-number">${n(j.orders)}</span><strong>commande${j.orders<=1?'':'s'} payée${j.orders<=1?'':'s'}</strong><span>par ${n(j.customers)} client${j.customers<=1?'':'s'} distinct${j.customers<=1?'':'s'}</span></div><div><span class="journey-number">${n(j.complete_mail)}</span><strong>avec tous les envois acceptés</strong><span>par le prestataire mail, pour les fichiers détaillés de l’achat</span></div><div><span class="journey-number">${n(j.email_access_orders)}</span><strong>avec un accès autorisé par lien mail</strong><span>pour ${n(j.email_access_customers)} client${j.email_access_customers<=1?'':'s'} · traces encore conservées</span></div></div>${j.orders?`<details class="journey-details"><summary>${j.orders===1?'Comprendre la commande et ses preuves':`Comprendre les ${n(j.orders)} commandes et leurs preuves`}</summary><dl class="journey-breakdown"><div><dt>Tous les envois acceptés</dt><dd>${n(j.complete_mail)}</dd></div><div><dt>Une partie des envois acceptée</dt><dd>${n(j.partial_mail)}</dd></div><div><dt>Suivi présent, aucune acceptation confirmée</dt><dd>${n(j.no_mail_acceptance)}</dd></div><div><dt>Sans trace de livraison dans ce suivi</dt><dd>${n(j.no_delivery_tracking)}</dd></div><div><dt>Sans détail des fichiers de l’achat</dt><dd>${n(j.no_item_detail)}</dd></div></dl><p class="fine">${n(j.accepted_files)} envoi(s) accepté(s) sur ${n(j.expected_files)} fichier(s) détaillé(s). Un bundle compte une commande ; ses fichiers sont vérifiés séparément. Une autorisation répétée compte une seule fois pour la commande.</p></details>`:'<p class="empty">Aucune commande ne répond à cette sélection. Les paiements de test, commandes remboursées ou contestées et comptes internes restent hors de ces chiffres.</p>'}${j.invalid_acceptances?`<p class="notice">${n(j.invalid_acceptances)} acceptation(s) avec une date incohérente : preuve écartée, à vérifier dans la fiche client.</p>`:''}${j.later_acceptances?`<p class="notice">${n(j.later_acceptances)} acceptation(s) datée(s) après ce relevé : non comptée(s). Actualisez pour relire leur état.</p>`:''}<p class="source-note">Ces mesures sont indépendantes : elles ne forment pas un taux de conversion. L’acceptation d’un mail ne prouve pas sa réception. L’autorisation d’un lien ne prouve ni le téléchargement complet, ni l’ouverture du fichier, ni l’identité du lecteur.</p><p class="source-note">Accès par lien : traces de ${j.retention_days} jours au maximum, depuis le ${date(j.retained_since)}. ${j.older_than_retention?n(j.older_than_retention)+' commande(s) sélectionnée(s) précèdent cette limite. ':''}Sans trace ne signifie pas sans accès : les accès depuis le compte ne sont pas attribués à un achat, et l’historique ancien n’est pas reconstitué. Les visites et débuts de paiement ne sont pas reliés à ces commandes.</p><a class="button" href="#clients">Consulter les clients et leurs achats →</a></section>`;
}
